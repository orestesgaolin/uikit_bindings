name: uikit_example
description: "Demonstrates how to use the uikit plugin."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  uikit_bindings:
    path: ../

  cupertino_icons: ^1.0.9
  objective_c: ^9.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

name: uikit_example
description: "Demonstrates how to use the uikit plugin."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  uikit_bindings:
    path: ../

  cupertino_icons: ^1.0.8
  objective_c: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
